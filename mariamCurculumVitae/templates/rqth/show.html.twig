{% extends 'base.html.twig' %}

{% block title %}Rqth{% endblock %}

{% block body %}
    <h1>Rqth</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rqth.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rqth.titre }}</td>
            </tr>
            <tr>
                <th>Rqth</th>
                <td>{{ rqth.rqth ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rqth_index') }}">back to list</a>

    <a href="{{ path('app_rqth_edit', {'id': rqth.id}) }}">edit</a>

    {{ include('rqth/_delete_form.html.twig') }}
{% endblock %}
