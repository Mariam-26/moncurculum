{% extends 'base.html.twig' %}

{% block title %}Poste{% endblock %}

{% block body %}
    <h1>Poste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poste.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ poste.titre }}</td>
            </tr>
            <tr>
                <th>Accroche</th>
                <td>{{ poste.accroche }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ poste.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_poste_index') }}">back to list</a>

    <a href="{{ path('app_poste_edit', {'id': poste.id}) }}">edit</a>

    {{ include('poste/_delete_form.html.twig') }}
{% endblock %}
