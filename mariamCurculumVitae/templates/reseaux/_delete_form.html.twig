<form method="post" action="{{ path('app_reseaux_delete', {'id': reseaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reseaux.id) }}">
    <button class="btn">Delete</button>
</form>
