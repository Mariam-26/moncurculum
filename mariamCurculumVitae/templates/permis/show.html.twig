{% extends 'base.html.twig' %}

{% block title %}Permis{% endblock %}

{% block body %}
    <h1>Permis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ permi.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ permi.titre }}</td>
            </tr>
            <tr>
                <th>Categories</th>
                <td>{{ permi.categories }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_permis_index') }}">back to list</a>

    <a href="{{ path('app_permis_edit', {'id': permi.id}) }}">edit</a>

    {{ include('permis/_delete_form.html.twig') }}
{% endblock %}
